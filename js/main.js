const $ = document.querySelector.bind(document)
const $$ = document.querySelectorAll.bind(document)

const contentList = $('.navContent_content_children2_button_text')
    contentList.forEach(function(content, index) {
        content.onclick
    });